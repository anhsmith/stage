% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_stage.R
\name{fit_stage}
\alias{fit_stage}
\title{Fit the STAGE (uniform + Gaussian) generative model}
\usage{
fit_stage(
  x,
  y,
  group = NULL,
  L = NULL,
  U = NULL,
  priors = stage_priors(x, y),
  chains = 4,
  iter = 2000,
  ...
)
}
\arguments{
\item{x}{Numeric vector of predictor values (e.g. length).}

\item{y}{Binary vector: 0 = immature, 1 = mature.}

\item{group}{Optional grouping variable (population). If NULL, all data
are treated as one population (J = 1).}

\item{L, U}{Truncation bounds for x. If NULL, use min(x) and max(x).}

\item{priors}{A list of prior hyperparameters created by \code{\link[=stage_priors]{stage_priors()}}.}

\item{chains, iter, ...}{Passed to \code{\link[cmdstanr:model-method-sample]{cmdstanr::sample()}}.}
}
\value{
An object of class "stage_fit".
}
\description{
Fit the STAGE (uniform + Gaussian) generative model
}
